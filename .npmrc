enable-pre-post-scripts=true
# astro-sienna ships with @astrojs/tailwind@6.0.2, whose peerDependencies still
# cap out at Astro 5. It works fine with the Astro 6.3.1 pinned in package.json,
# but npm's strict resolver refuses to install without this flag.
legacy-peer-deps=true