<button class="theme-toggle" id="theme-toggle" type="button" aria-label="Cambiar tema">
	<span class="icon" aria-hidden="true"></span>
	<span class="label"></span>
</button>

<style is:global>
	.theme-toggle {
		display: inline-block;
		padding: 6px 12px;
		border-radius: 999px;
		background: hsl(var(--theme-bg) / 0.82);
		backdrop-filter: blur(8px);
		-webkit-backdrop-filter: blur(8px);
		color: hsl(var(--theme-text-muted));
		border: 1px solid var(--hairline);
		font-family: var(--font-mono);
		font-size: 11.5px;
		font-weight: 500;
		line-height: 1.4;
		cursor: pointer;
		box-shadow: 0 2px 10px rgba(24, 22, 17, 0.06);
		transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
	}
	.theme-toggle:hover {
		background: hsl(var(--theme-bg));
		border-color: hsl(var(--theme-accent) / 0.5);
		color: hsl(var(--theme-accent));
	}
	.theme-toggle:focus-visible {
		outline: 2px solid hsl(var(--theme-accent));
		outline-offset: 3px;
	}

	.theme-toggle .icon {
		display: inline-block;
		width: 1em;
		font-size: 13px;
		margin-right: 6px;
		text-align: center;
		transform: translateY(-1px);
	}
	.theme-toggle .icon::before { content: "☾"; }
	[data-theme="dark"] .theme-toggle .icon::before { content: "☀"; }

	.theme-toggle .label {
		display: inline-block;
		min-width: 3.6em;
		text-align: left;
	}
	.theme-toggle .label::before { content: "Oscuro"; }
	[data-theme="dark"] .theme-toggle .label::before { content: "Claro"; }
</style>

<script>
	const r = document.documentElement;
	const btn = document.getElementById("theme-toggle") as HTMLButtonElement | null;
	if (btn) {
		btn.addEventListener("click", () => {
			const next = r.dataset.theme === "dark" ? "light" : "dark";
			r.dataset.theme = next;
			try { localStorage.setItem("theme", next); } catch {}
			document.dispatchEvent(new CustomEvent("theme-change", { detail: { theme: next } }));
		});
	}
</script>
